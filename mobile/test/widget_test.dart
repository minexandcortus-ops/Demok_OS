// Smoke test minimal — vérifie que les tests peuvent s'exécuter.
// Les vrais tests UI doivent être écrits avec des mocks backend.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test — placeholder', (WidgetTester tester) async {
    // TODO: Implémenter des tests avec mocks du backend
    expect(true, isTrue);
  });
}
